name: test auto action

on: 
  push: 
    branches: 
      - main
      - test
  workflow_dispatch:

jobs:
  test-echo:
    runs-on: ubuntu-latest

    steps:
    - name: test-echo
      run: echo "11"
    

  
